# Joke Jar

A friendly random joke generator powered by [JokeAPI](https://jokeapi.dev/). Click the button to fetch a safe-for-work joke, then copy it with one click.

## Run locally

Open `index.html` in a browser, or serve the directory with any static web server. The app uses the JokeAPI endpoint directly from the browser and needs an internet connection to fetch jokes.
